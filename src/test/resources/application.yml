server:
  port: 8888

spring:
  datasource:
      driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    open-in-view: false
    show-sql: false
    hibernate:
      ddl-auto: none

springdoc:
  paths-to-match:
    - /persons/**
    - /books/**
  swagger-ui:
    use-root-path: true
